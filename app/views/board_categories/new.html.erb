<h2><%= l(:label_new) %></h2>

<% labelled_tabular_form_for :board_category, @board_category, :url => {:action => 'create'} do |f| %>
  <%= render :partial => 'form', :locals => {:f => f} %>
  <%= submit_tag l(:button_commit) %>
<% end %>
